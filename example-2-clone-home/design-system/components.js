window.SiohiomaUI=(()=>{const escape=value=>String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const Icon=name=>SiohiomaIcons.render(name);const Button=({label='',className='',icon,attributes=''}={})=>`<button type="button" class="${escape(className)}" ${attributes}>${icon?Icon(icon):''}${escape(label)}</button>`;const SidebarItem=({href='#',label,icon='dashboard',active=false}={})=>`<a href="${escape(href)}"${active?' class="active" aria-current="page"':''} aria-label="${escape(label)}">${Icon(icon)}<span class="nav-text">${escape(label)}</span></a>`;const KPICard=({title,value,detail='',trend='',tone='positive'}={})=>`<article class="metric kpi-card"><div><p>${escape(title)}</p>${Button({className:'detail-arrow',icon:'arrow',attributes:`aria-label="View ${escape(title)} details"`})}</div><h2>${escape(value)}</h2><small class="${tone==='negative'?'down':'up'}">${escape(trend)} <i>${escape(detail)}</i></small></article>`;const Panel=({title,className='',actionsHtml='',contentHtml=''}={})=>`<article class="panel ${escape(className)}"><div class="title"><h2>${escape(title)}</h2>${actionsHtml}</div>${contentHtml}</article>`;const StatusBadge=({label,tone}={})=>`<span class="ds-status ${escape(tone||label.toLowerCase())}">${escape(label)}</span>`;const TransactionRow=({icon,name,date,status,reference}={})=>`<div class="transaction"><span class="product-icon">${escape(icon)}</span><span class="transaction-copy"><strong>${escape(name)}</strong><small>${escape(date)}</small></span><span class="transaction-status">${StatusBadge({label:status,tone:status.toLowerCase()})}<small>${escape(reference)}</small></span></div>`;return{escape,Icon,Button,SidebarItem,KPICard,Panel,StatusBadge,TransactionRow}})();
